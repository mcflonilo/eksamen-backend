spring:
  datasource:
    url: jdbc:h2:mem:mydb
    username: lars
    password:
    driverClassName: org.h2.Driver
    generate-unique-name: false
    name: exam-backend-test
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: update